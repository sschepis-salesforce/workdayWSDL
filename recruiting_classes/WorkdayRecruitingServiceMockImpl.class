//Generated by FuseIT WSDL2Apex (http://www.fuseit.com/Solutions/SFDC-Explorer/Help-WSDL-Parser.aspx)

@isTest
public class WorkdayRecruitingServiceMockImpl implements WebServiceMock {
	public void doInvoke(
			Object stub,
			Object request,
			Map<String, Object> response,
			String endpoint,
			String soapAction,
			String requestName,
			String responseNS,
			String responseName,
			String responseType) {
       
		System.debug(LoggingLevel.INFO, 'WorkdayRecruitingServiceMockImpl.doInvoke() - ' +
			'\n request: ' + request +
			'\n response: ' + response +
			'\n endpoint: ' + endpoint +
			'\n soapAction: ' + soapAction +
			'\n requestName: ' + requestName +
			'\n responseNS: ' + responseNS +
			'\n responseName: ' + responseName +
			'\n responseType: ' + responseType);

		if(request instanceOf WorkdayRecruiting.Get_Candidates_RequestType) {
			response.put( 'response_x', new WorkdayRecruiting.Get_Candidates_ResponseType());
		}
		else if(request instanceOf WorkdayRecruiting.Get_Job_Application_Additional_Data_RequestType) {
			response.put( 'response_x', new WorkdayRecruiting.Get_Job_Application_Additional_Data_ResponseType());
		}
		else if(request instanceOf WorkdayRecruiting.Get_Job_Postings_RequestType) {
			response.put( 'response_x', new WorkdayRecruiting.Get_Job_Postings_ResponseType());
		}
		else if(request instanceOf WorkdayRecruiting.Server_Timestamp_GetType) {
			response.put( 'response_x', new WorkdayRecruiting.Server_TimestampType());
		}
	}
}
